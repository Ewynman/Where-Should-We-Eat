/**
 * Where Should We Eat? - Next.js Config
 * TODO: Add rewrites/proxy to FastAPI backend if needed (e.g. /api -> http://localhost:8000)
 * TODO: Add env validation for NEXT_PUBLIC_API_URL
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
