# Where Should We Eat? - Frontend Environment
# Copy to .env.local and adjust as needed
NEXT_PUBLIC_API_URL=http://localhost:8000
# Set to "true" to use mock API when backend is not ready
# NEXT_PUBLIC_USE_MOCK=true
